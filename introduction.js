/*
1. What is node js
    Node.js is open source and cross-platform runtime time environment.
    It uses V8 JavaScript engine, which is the core of Google Chrome. V8 makes node application 
    has high performance.
    Node js app runs in a single process, without creating new thread for every request.
    provides asynchronous I/O,


2. when to use node.js
3. how to use it?

basic concept
modules: the same as libraries in js, it is the function you want to use.
    built-in modules
    customized modules-use exports to export properties and functions
                    use require to import:  var dt = require('./myfirstmodule');
*/
